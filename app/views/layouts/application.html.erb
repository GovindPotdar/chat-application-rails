<!DOCTYPE html>
<html>
  <head>
    <title>ChatApplication</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= render "shared/stylesheet" %>
  </head>

  <body>
    <%= render "shared/header" %>
    <%= yield %>
  </body>
    <%= render "shared/script" %>
   
</html>
